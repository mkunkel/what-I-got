#show_electronic.electronics
  .back_button= link_to("Return to electronics", {action: "index"})
  %h2 Name: #{@electronic.name}
  %h4 Serial number: #{@electronic.serial}
  -# .edit= link_to("Edit",  {action: "edit", id: @electronic.id})
  .delete= link_to("Delete",  delete_collection_electronic_path(id: @electronic.id, collection_id: @collection.id))
