#delete_container.books
  .back_button= link_to("Return to books", {action: "index"})
  %h2 Delete book
  %p Are you certain you want to delete #{@book.title}?
  = button_to("Delete book", collection_book_path(id: @book.id, collection_id: @collection.id), method: :delete)
