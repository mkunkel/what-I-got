#show_movie.movies
  .back_button= link_to("Return to movies", {action: "index"})
  %h2 "#{@movie.title}"
  %h4 Director: #{@movie.director}
  %h4 Genre: #{@movie.genre}
  %h4 Rating: #{@movie.rating}
  -# .edit= link_to("Edit",  {action: "edit", id: @movie.id})
  -# .delete= link_to("Delete",  {action: "delete", id: @movie.id})
