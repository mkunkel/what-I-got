#show_movie.movies
  .back_button= link_to("Return to movies", collection_movies_path(collection_id: @collection.id))
  %h2 "#{@movie.title}"
  %h4 Director: #{@movie.director}
  %h4 Genre: #{@movie.genre}
  %h4 Rating: #{@movie.rating}
  -# .edit= link_to("Edit",  {action: "edit", id: @movie.id})
  .delete= link_to("Delete",  delete_collection_movie_path(id: @movie.id, collection_id: @collection.id))
